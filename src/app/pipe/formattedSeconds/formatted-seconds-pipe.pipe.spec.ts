import { FormattedSecondsPipePipe } from './formatted-seconds-pipe.pipe';

describe('FormattedSecondsPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FormattedSecondsPipePipe();
    expect(pipe).toBeTruthy();
  });
});
